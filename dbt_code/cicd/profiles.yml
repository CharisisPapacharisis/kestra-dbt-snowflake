dbt_code:
  outputs:
    dev:
      account: ofgerpx-no40481
      database: MY_DB
      password: "{{ secret('SNOWFLAKE_PASSWORD')}}"
      role: ACCOUNTADMIN
      schema: MY_SCHEMA
      threads: 1
      type: snowflake
      user: "{{ secret('SNOWFLAKE_USER')}}"
      warehouse: COMPUTE_WH
  target: "{{ secret('DBT_TARGET', 'dev')}}"